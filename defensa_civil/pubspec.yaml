name: app_minerd
description: Aplicación para técnicos del MINERD.

publish_to: 'none' # Indica que este paquete no se publicará.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^5.0.0
  sqflite: ^2.0.0+3
  path_provider: ^2.0.2
  path: ^1.8.0
  geolocator: ^7.6.2
  google_maps_flutter: ^2.0.6
  http: ^0.13.3
  url_launcher: ^6.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/tu_foto.jpg
